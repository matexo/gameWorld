version: '2'
services:
    gameworld-mysql:
        container_name: gameworld-mysql
        image: mysql:5.7.13
        # volumes:
        #     - ~/volumes/jhipster/gameWorld/mysql/:/var/lib/mysql/
        environment:
            - MYSQL_USER=root
            - MYSQL_ALLOW_EMPTY_PASSWORD=yes
            - MYSQL_DATABASE=gameworld
        ports:
            - 3306:3306
        command: mysqld --lower_case_table_names=1 --skip-ssl
